import * as React from 'react';

const ReactCounter = () => {
  return (
    <div>
      <h1>React Counter</h1>
    </div>
  );
};

ReactCounter.displayName = 'ReactCounter';

export default ReactCounter;
